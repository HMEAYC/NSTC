# HMEAYC 系統未完成項目總覽

> **報告日期**：2026-07-15
> **專案名稱**：即時 AI 音樂學習工具之研發、實作與成效評估：支持幼兒整合性發展
> **執行單位**：朝陽科技大學

---

## 一、已全部完成的模組

| 模組 | 狀態 |
|------|------|
| 韌體（ESP32-C3 + MPU6500 IMU） | ✅ 完成 |
| 後端 WebSocket 即時串流 | ✅ 完成 |
| 節奏分析 `rhythm.py` | ✅ 完成（文件標為 stub，實際已實作） |
| Freeze Dance 分析 `freeze_dance.py` | ✅ 完成（同上） |
| 巨觀分析 `macro.py`（隊形/熱區/參與度） | ✅ 完成 |
| 微觀分析 `micro.py`（追蹤/流暢度） | ✅ 完成 |
| 指標燈號 `metrics.py` | ✅ 完成 |
| 跨模態裝置配對（FFT + Hungarian） | ✅ 完成 |
| 課程管理（CRUD + 開始/結束 + 評分） | ✅ 完成 |
| 多租戶 RBAC（4 角色 + JWT + org_id 隔離） | ✅ 完成 |
| Dashboard 21 個頁面 | ✅ 完成 |
| Docker Compose 一鍵啟動 | ✅ 完成 |
| OTA 韌體更新 | ✅ 完成 |
| 教案 PDF 匯入工具 | ✅ 完成 |

---

## 二、尚未完成 / 部分完成的項目

### 高優先

| 項目 | 位置 | 說明 |
|------|------|------|
| **節奏/凍結分析需音樂源整合** | `AssessmentIndicators.tsx:267,271` | 後端演算法已就緒，但即時串流模式缺少音樂音訊輸入（需麥克風或同步音樂播放），導致節奏同步誤差與凍結反應顯示 `--` |
| **音樂停止信號偵測** | `freeze_dance.py` | 需音樂 RMS 能量做為停止信號輸入，目前僅能處理影片分析模式（已有音訊檔），即時模式無法觸發 |

### 中優先

| 項目 | 位置 | 說明 |
|------|------|------|
| **人臉辨識為輕量替代** | `backend/app/tracking/face_insight.py:35` | 使用 HOG 特徵（128 維）替代 ArcFace/insightface，辨識品質較低，`insightface` 套件尚未整合 |
| **4 項 CV 指標無後端** | `AssessmentIndicators.tsx:286-306` | 步態對稱性、平衡搖擺面積、上下肢協調、社會/情緒/專注 — 僅 UI 占位符，無對應後端演算法 |
| **6 項攝影機指標為占位** | `AssessmentIndicators.tsx` | 團體投入度、隊形穩定度、空間利用率等 — 需 YOLO + MediaPipe 即時管線，目前僅影片分析模式有 |

### 低優先（文件不一致）

| 項目 | 位置 | 說明 |
|------|------|------|
| **Dashboard 頁面文件不全** | `OPERATION.md §5.1` | 只列 12 個路徑，實際有 21 個路由（缺 login、register、accept-invite、firmware、wifi、parent 等） |
| **API 參考表不全** | `OPERATION.md §8.1` | 缺少 invite 流程、start/end、activity、評估相關端點 |
| **電量計算基準未對齊** | `firmware/battery.c` vs `hardware/README.md` | 韌體用 3300mV~4200mV 範圍，硬體文件另定義 3.05V 警告 / 2.80V brownout，兩套門檻未對應 |
| **企劃書演算法描述過度** | `CM03-Fianl(2).docx` | 企劃書提到 HT-Demucs、Bi-LSTM、Kalman filter、TinyML，實際用 FFT+RMS（MVP 簡化版），尚未修正 |
| **通訊架構描述不符** | 企劃書部分段落 | 部分仍提 ESP-NOW/UDP 雙向通訊，實際為 WiFi + WebSocket 單向上傳 |
| **API client 重複宣告** | `dashboard/src/api/client.ts` | `listSessions`、`createSession`、`getSession`、`endSession` 各宣告兩次（前後版本），需清理 |
| **未定義型別參考** | `dashboard/src/api/client.ts:391` | 引用 `CourseTemplateInfo` 型別但未定義（實際名為 `SessionTemplateInfo`） |
| **開發計畫文件過時** | `.opencode/plans/development-plan.md` | 仍標註 `rhythm.py`、`freeze_dance.py` 為 stub，但已全數實作 |

---

## 三、場域測試前待辦

根據 README 里程碑（2027/01～03 場域測試）：

1. **IRB 送審文件** — 家長同意書、個資告知書、研究說明書（Liza 主責）
2. **硬體採購** — ESP32-C3 模組×10、MPU6500×10、電池、PCB 打樣
3. **場域 WiFi 環境建置** — 路由器 + 教室網路拓撲驗證
4. **音樂播放同步機制** — 即時模式下如何同步音樂與 IMU 資料流（關鍵缺口）

---

## 四、總結

系統核心功能（MVP）已完成度約 **90%**。主要缺口在：

- **即時音樂源整合**：影響節奏/凍結指標的即時顯示
- **攝影機管線的即時化**：目前僅影片分析模式可用
- **文件一致性**：數處文件需更新以反映實際實作

---

*報告由 opencode 自動產生*
